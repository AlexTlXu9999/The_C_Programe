#include<stdio.h>
#define LOWER 0
#define UPPER 300
#define STEP 20

int main() {
	float c;
	for (int i = 0; i < UPPER; i = i + STEP) {
		c = 5.0 * (i - 32) / 9.0;
		printf("%d\t%3f\n", i, c);
	}
	return 0;
}
